import React from 'react';
import Header from './Header';

const RoleBasedHeader = () => {
    return (
        <div>
            <Header></Header>
        </div>
    );
};

export default RoleBasedHeader;